# Automatic HTTPS reverse proxy for the ATANOR Cloud Brain.
# Replace cloud.yourdomain.com with your domain (A record -> this VPS IP).
# Caddy fetches/renews a real Let's Encrypt cert for you, no config needed.

cloud.yourdomain.com {
    encode zstd gzip
    reverse_proxy atanor-cloud:8500
}
